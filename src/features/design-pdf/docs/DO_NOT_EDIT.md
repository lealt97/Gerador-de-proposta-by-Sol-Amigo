# Manutenção

Para alterar o Design PDF, prefira editar `src/features/design-pdf`.

O arquivo `src/lib/pdf/utils/coverSvgEngine.ts` é mantido apenas como adaptador de compatibilidade para chamadas antigas de `buildCoverSvg`.
