# Notas de teste

O repositório não possui checks automáticos expostos pelo GitHub nesta etapa. A validação recomendada é:

```bash
npm install
npm run lint
npm run dev
```

Depois acesse:

```txt
http://localhost:3000/design-pdf
```

E execute o roteiro em `QA_CHECKLIST.md`.
